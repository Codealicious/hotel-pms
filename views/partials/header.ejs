<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css?family=Spartan:500&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Abril+Fatface&display=swap" rel="stylesheet">
    <% if(links) links.forEach( (link) => { %>
    <%-   `<link href="/stylesheets/${link}" rel="stylesheet" type="text/css" >` %>
    <%  }); %>
    <% if(scripts) { scripts.forEach( (script) => { %>
    <%-  `<script defer src="/javascripts/${script}"></script>`%>
    <%  });} %>
    <title><%= title %></title>
</head>
<body>
    <main>

    